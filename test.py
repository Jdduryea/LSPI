# f = lambda a: np.dot(_compute_phi(s, a, basis_functions), w)

# import numpy as np
# # from scipy.optimize import minimize
# # f = lambda x: x[0]+ x[1] + x[2]
# # x0 = [0,0,0]
# #
# #
# # result = minimize(f, x0, method='L-BFGS-B', options={'xtol': 1e-8, 'disp': True}, bounds = [(-2*np.pi,2*np.pi),(-2*np.pi,2*np.pi),(-2*np.pi,2*np.pi)])
# # print (result.x)

print(5/2)